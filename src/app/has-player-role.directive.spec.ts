import { HasPlayerRoleDirective } from './has-player-role.directive';

describe('HasPlayerRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPlayerRoleDirective();
    expect(directive).toBeTruthy();
  });
});
